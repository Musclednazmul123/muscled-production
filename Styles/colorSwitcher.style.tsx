import styled from 'styled-components';

export const CustomButton = styled.button`
  display: flex;
  cursor: pointer;
  align-items: center;
  border: 0;
  width: 4rem;
  height: 4rem;
  background: transparent;

  svg {
    color: var(--logoColor);
  }
`;
